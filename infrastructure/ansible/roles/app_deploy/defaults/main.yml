---
# Default values for app_deploy role
operation: install
validate_deployment: true
debug_mode: false

# Default application configuration
default_app_config:
  enabled: true
  timeout: 300
  readiness_timeout: 300
  validate_deployment: true
  validate_service: false
  remove_namespace: true
  values: {}
