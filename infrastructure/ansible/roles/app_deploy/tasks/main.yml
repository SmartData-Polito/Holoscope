---
- name: Load application variables
  include_vars:
    file: "{{ inventory_dir }}/group_vars/{{ application }}.yml"
    name: app_config
  failed_when: false
  register: var_load_result

- name: Fail if application config not found
  fail:
    msg: "Configuration file not found for application: {{ application }}"
  when: var_load_result.failed | default(false)

- name: Set application facts
  set_fact:
    app_name: "{{ application }}"
    app_namespace: "{{ app_config.namespace | default(application) }}"
    chart_path: "{{ app_config.chart_path }}"
    app_enabled: "{{ app_config.enabled | default(true) | bool }}"
    helm_values: "{{ app_config['values'] | default({}) }}"

- name: Debug application configuration
  debug:
    msg:
      - "Application: {{ app_name }}"
      - "Namespace: {{ app_namespace }}"
      - "Chart Path: {{ chart_path }}"
      - "Operation: {{ action | default('install') }}"
      - "Enabled: {{ app_enabled }}"
  when: debug_mode | default(false) | bool

- name: Ensure namespace exists
  kubernetes.core.k8s:
    name: "{{ app_namespace }}"
    api_version: v1
    kind: Namespace
    state: present
  when: app_enabled

- name: Deploy Helm chart for {{ app_name }} (fresh install)
  kubernetes.core.helm:
    name: "{{ app_name }}"
    chart_ref: "{{ chart_path }}"
    namespace: "{{ app_namespace }}"
    create_namespace: false
    state: present
    wait: true
    values: "{{ helm_values }}"
  when: 
    - app_enabled
    - (action | default('install')) == 'install'
  register: helm_result    

- name: Remove Helm chart for {{ app_name }}
  kubernetes.core.helm:
    name: "{{ app_name }}"
    namespace: "{{ app_namespace }}"
    state: absent
    wait: true
    timeout: "{{ app_config.timeout | default(300) }}"
  when: 
    - app_enabled
    - (action | default('install')) == 'remove'
  register: helm_remove_result

- name: Display deployment result
  debug:
    msg: "{{ 'Successfully deployed' if helm_result.changed else 'No changes needed for' }} {{ app_name }}"
  when: 
    - app_enabled
    - (action | default('install')) == 'install'
    - helm_result is defined