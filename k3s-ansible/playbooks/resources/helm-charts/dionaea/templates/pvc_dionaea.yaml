apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-tcpdump-dionaea
  namespace: hpot
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi   

