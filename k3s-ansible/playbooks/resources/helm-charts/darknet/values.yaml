deploymentName: darknet
appName: darknet
hostNetwork: true
namespace: darknetns

containers:
  tcpdump:
    image: nicolaka/netshoot
    imagePullPolicy: IfNotPresent
    mountPath: /data
    command:
      - "/bin/bash"
      - "-c"
    interface: eth0
    rotate_seconds: 3600
    filter: "ip and (net 10.10.0.0/24)"
    outputPath: "/data/trace-%Y%m%d_%H-%M-%S_%s.pcap"

nodeAffinity:
  hostname:
    values:
      - poli-worker-00

volumes:
  pv:
    name: pv-volume-tcpdump-darknet
    storageClassName: manual
    capacity: 5Gi
    accessModes:
      - ReadWriteOnce
    hostPath:
      path: "/data"
  pvc:
    name: pvc-tcpdump-darknet
    storageClassName: manual
    accessModes:
      - ReadWriteOnce
    resources:
      requests:
        storage: 5Gi
