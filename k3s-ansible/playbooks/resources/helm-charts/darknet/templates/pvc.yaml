apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-tcpdump-darknet 
 # namespace: hpot
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi   

