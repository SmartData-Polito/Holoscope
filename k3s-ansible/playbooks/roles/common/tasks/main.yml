---
- name: Update apt packages
  apt:
    update_cache: yes
    upgrade: no
  become: yes

- name: Install necessary dependencies
  apt:
    name: "{{ item }}"
    state: present
  with_items:
    - curl
    - apt-transport-https
    - net-tools
  become: yes
