- name: Uninstall Helm chart for Arp Responder on each agent node
  command: >
    helm uninstall arp-responder-ansible-{{ item }} --namespace  {{ hostvars[item].arp_responder_namespace }}
  environment:
    KUBECONFIG: "{{ fetch_result.dest }}"
  with_items: "{{ groups['arp_responder'] }}"
  ignore_errors: yes
        
- name: Remove the namespace for Arp Responder
  command: >
    kubectl delete namespace {{ hostvars[groups['arp_responder'][0]].arp_responder_namespace }}
  environment:
    KUBECONFIG: "{{ fetch_result.dest }}"
  run_once: true
  ignore_errors: yes
