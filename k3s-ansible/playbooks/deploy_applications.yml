---
# ansible-playbook -i inventory/hosts.yml --ask-vault-password playbooks/deploy_applications.yml  --ask-become-pass
- name: Copy and modify file from remote host
  hosts: localhost
  vars:
    arp4_interface: "eth0"
    arp6_interface: "eth0"
  tasks:
    - name: Fetch Kubeconfig from the remote host
      fetch:
        src: /etc/rancher/k3s/k3s.yaml
        dest: ~/.kube/kubeconfig.yaml
        flat: yes
      delegate_to: "{{ groups['master'][0] }}"
      register: fetch_result

    - name: Change Kubeconfig permissions
      ansible.builtin.file:
        path: "{{ fetch_result.dest }}"
        mode: "go-r"

    - name: Ensure yq is installed on localhost
      become: true
      apt:
        name: yq
        state: present
      run_once: true

    - name: Modify the server URL in the YAML file using yq
      shell: |
        yq -yi '.clusters[0].cluster.server = "https://{{ hostvars[groups['master'][0]]['ansible_host'] }}:6443"' {{ fetch_result.dest }}
      run_once: true

    - name: Ensure Helm is installed
      command: helm version
      register: helm_version
      changed_when: False
      failed_when: "'version' not in helm_version.stdout"

    - name: Install Helm chart for ARP Responder with dynamic interfaces
      command: >
        helm upgrade -i arp-ansible {{ playbook_dir }}/resources/helm-charts/arp-responder --namespace my-namespace --create-namespace \
        --set containers.arp4.interface=eth1 \
        --set containers.arp4.network="20.0.0.0/24" \
        --set containers.arp6.interface=eth1 \
        {% for host in groups['agents'] %}
          --set nodeAffinity.hostname.values[{{ loop.index - 1}}]={{ host }}
        {% endfor %}
      environment:
        KUBECONFIG: "{{ fetch_result.dest }}"
