apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "udpresponder.fullname" . }}-env
  namespace: {{ .Values.global.namespace }}
data:
  .env: |
{{ .Files.Get "files/udpresponder.env" | indent 4 }}