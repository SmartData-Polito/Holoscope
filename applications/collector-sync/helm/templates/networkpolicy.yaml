{{- if .Values.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "darknet.fullname" . }}
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "darknet.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "darknet.selectorLabels" . | nindent 6 }}
  
  policyTypes:
  - Ingress
  - Egress
  
  # Allow ingress from specific CIDRs
  ingress:
  {{- range .Values.networkPolicy.allowedCidrs }}
  - from:
    - ipBlock:
        cidr: {{ . }}
  {{- end }}
  
  # Allow egress to worker nodes for SSH collection
  egress:
  - to: []
    ports:
    - protocol: TCP
      port: 65222
  # Allow DNS resolution
  - to: []
    ports:
    - protocol: UDP
      port: 53
    - protocol: TCP
      port: 53
{{- end }}