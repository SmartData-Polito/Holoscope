{{- if .Values.collector.enabled }}
---
# Persistent Volume for Collector
apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ include "collector-sync.fullname" . }}-collector-pv
  labels:
    {{- include "collector-sync.collector.labels" . | nindent 4 }}
    type: local
spec:
  storageClassName: {{ .Values.global.storageClass }}
  capacity:
    storage: {{ .Values.collector.storage.size }}
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: {{ .Values.collector.storage.hostPath }}
    type: Directory
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: node-role.kubernetes.io/control-plane
          operator: Exists

---
# Persistent Volume Claim for Collector
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "collector-sync.fullname" . }}-collector-pvc
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "collector-sync.collector.labels" . | nindent 4 }}
spec:
  storageClassName: {{ .Values.global.storageClass }}
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.collector.storage.size }}
{{- end }}